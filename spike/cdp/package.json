{
  "name": "icarus-spike-cdp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Disposable spike: prove CDP-over-Metro feasibility (E-Spike-CDP). Not production code.",
  "engines": {
    "node": ">=21"
  },
  "scripts": {
    "discover": "node src/discover.js",
    "connect": "node src/connect.js",
    "capabilities": "node src/capabilities.js",
    "proxy": "node src/proxy.js"
  },
  "comment": "Phases 1/2/4 are zero-dependency (built-in fetch + WebSocket). Phase 3 (proxy) needs `ws` because Node has no built-in WebSocket server.",
  "optionalDependencies": {
    "ws": "^8.18.0"
  }
}
