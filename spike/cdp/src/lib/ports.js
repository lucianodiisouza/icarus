// Default ports where Metro's inspector proxy is commonly found.
// 8081 = default Metro; 8082+ = additional/second instances; 19000-19002 = Expo.
// These are starting points to VERIFY, not guarantees (see spike plan Phase 1).
export const DEFAULT_METRO_PORTS = [8081, 8082, 19000, 19001, 19002];

export const DEFAULT_HOST = "localhost";
